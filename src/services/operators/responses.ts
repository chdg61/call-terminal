export interface ILoadListOperatorsResponse {
    id: number;
    slug: string;
    name: string;
}

export interface ILoadDetailOperatorResponse {
    id: number;
    slug: string;
    name: string;
}
